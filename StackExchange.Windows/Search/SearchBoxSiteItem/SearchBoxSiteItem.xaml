<UserControl
    x:Class="StackExchange.Windows.Search.SearchBoxSiteItem.SearchBoxSiteItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StackExchange.Windows.Search.SearchBoxSiteItem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="ItemGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image x:Name="SiteIcon"
               Grid.Column="0"
               Grid.Row="0"
               Grid.RowSpan="2"
               Margin="0,0,8,0"/>

        <TextBlock x:Name="SiteName"
                   Grid.Column="1"
                   Grid.Row="0"/>

        <TextBlock x:Name="SiteDescription"
                   Grid.Column="1"
                   Grid.Row="1"
                   FontSize="12"/>
    </Grid>
</UserControl>
